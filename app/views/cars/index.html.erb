<h1>Cars</h1>

<ul>
  <% @cars.map do |car| %>
    <li><%= link_to car.model, new_car_order_url(car_id: car.id) %>
      <br><%=car.price%>
      <br><%= car.status %></li>
  <% end %>
  <br>
  <%= link_to "Add new car", new_car_path  %>

</ul>

